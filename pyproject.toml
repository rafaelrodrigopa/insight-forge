[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"


[project]
name = "insight-forge"
version = "0.1.0"
description = "AI agents platform for content intelligence"
requires-python = ">=3.12"


[tool.setuptools.packages.find]
where = ["."]